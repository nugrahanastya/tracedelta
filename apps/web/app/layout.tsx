import type { ReactNode } from 'react';
import './style.css';
export const metadata={title:'TraceDelta | Evidence-based material revision review',description:'Synthetic engineering document change review demo'};
export default function RootLayout({children}:{children:ReactNode}){return <html lang="en"><body>{children}</body></html>}
